# SDK audit: A3B4-A470

Status: AUDIT_COMPLETE / PUBLIC_SURFACE_COHERENT

This pass compares the consolidated ABI inventory for A3B4-A470 against the public umbrella header `os3k/os3k.h` on branch `sdk/abi-automation`.

## Closed contracts currently exposed

The umbrella header exposes only the following late NEO13-only contracts:

- `uint16_t SYS_A3B4(void *record);`
- `uint8_t SYS_A3B8(uint16_t arg1, void *arg2, uint16_t arg3, uint8_t arg4);`
- `uint8_t SYS_A3BC(void);`
- `uint8_t SYS_A3C0(void);`
- `uint8_t SYS_A404(void);`
- `void *SYS_A41C(uint8_t selector);`
- `char *SYS_A438(char *dst);`
- `uint16_t SYS_A470(void);`

All eight are classified `CERRADA` in the consolidated inventory. Their source/vendor names remain unknown where noted, but the callable mechanical contracts are sufficiently closed for public neutral `SYS_Axxx` exposure.

## Evidence-limited range correctly excluded

No umbrella declarations were found for the entries currently classified `CERRADA_CON_LÍMITE_DE_EVIDENCIA`, including:

- A3C4-A400 except A404
- A408-A418
- A420-A434
- A43C-A454

Those entries have reconstructed mechanics but unresolved return contractuality, output ABI form, semantic type, or other source-level contract limits. They should remain outside the umbrella header unless new evidence resolves the specific limit.

## Non-callable gap correctly excluded

A458, A45C, A460 and A464 are `NO_SYSCALL/NO_CALLABLE` and are correctly absent from the public header.

## A468 and A46C

A468 and A46C are both classified `CERRADA` in the consolidated inventory, but they remain intentionally absent from `os3k.h`. This matches the prior publication policy: their mechanics are usable and documented, but there is no current SDK consumer requirement to expose them publicly. Do not add them merely for contiguous-number completeness.

## Result

No accidental promotion of an evidence-limited NEO13 trap into the umbrella SDK surface was found in A3B4-A470.

Therefore this late range does **not** require an `os3k.h` correction batch at this time. The remaining SDK audit work is concentrated in earlier public-header gaps and documentation reconciliation already recorded separately.
